function callpassey(depth,GR,ILD,DT,LOM,RTbase,DTbase,ndata)

DLog=[];
for i=1:ndata;
    if GR(i)>-999.25
    elseif ILD(i)>-999.25
    elseif DT(i)>-999.25
        DLog(i)=log10*(ILD(i)/RTbase)+0.02*(DT(i)-DTbase);
    else
        DLog(i)==0;
    end
    DLogR=[D
end